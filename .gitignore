# =========================================================
# Nix / flake artifacts
# =========================================================
/result
/result-*
/.direnv/

# devenv: ephemeral environment store (NOT the lockfile — commit devenv.lock)
/.devenv/

# =========================================================
# Secrets — never commit plaintext keys/creds.
# Encrypted values live in secrets/ (managed by sops).
# =========================================================
age-keys.txt
*.age
*.gpg

# =========================================================
# Language / dependency artifacts
# =========================================================
node_modules/
*.o
*.a

# =========================================================
# OS / editor cruft
# =========================================================
core
core.*
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
