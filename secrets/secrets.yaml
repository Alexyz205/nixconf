# PLACEHOLDER - do not leave plaintext in this file.
#
# sops template. Encrypt it once the host age key is registered in .sops.yaml:
#   sops updatekeys secrets/secrets.yaml
# The file then becomes encrypted; commit it as-is.
#
# Sample secret for testing the pipeline. Reference it from
# modules/secrets.nix (sops.secrets."sample-secret") to validate end-to-end:
sample-secret: changeme