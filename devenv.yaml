# terraform is BSL-licensed (unfree in nixpkgs), so allow unfree packages.
allowUnfree: true

inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
  treefmt-nix:
    url: github:numtide/treefmt-nix